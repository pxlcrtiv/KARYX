# AGENTS

Working notes for collaborative coding agents on this repo.

## Agent skills

### Issue tracker

GitHub Issues for `pxlcrtiv/KARYX`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage` · `needs-info` · `ready-for-agent` · `ready-for-human` · `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: future `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
